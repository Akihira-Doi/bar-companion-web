# Bar Companion 音声入出力ノウハウ

## 1. 対象

Bar Companionで、AIの読み上げ（TTS）終了後にブラウザの音声認識を再開し、複数ターンの会話を安定して続けるための知見をまとめる。

このアプリには別々の2つの音声機構がある。

- AIの声を鳴らす：JavaScriptの`Audio`とOpenAI TTSのMP3
- 利用者の声を文字にする：Web Speech APIの`SpeechRecognition`

問題が起きやすいのは、スピーカー出力からマイク入力へ切り替える瞬間である。

## 2. TTSごとに新しいAudioオブジェクトを使う

以前は1個の`Audio`を複数回の回答で再利用していた。この方法では、以前のMP3、イベント、停止状態、端末の音声出力状態が次の再生へ残る可能性がある。

現在は、最初にユーザー操作で再生許可を得るための`Audio`だけ再利用し、回答の再生が終わるたびに参照を破棄する。次の回答では新しい`Audio`を作る。

期待する効果：

- 前回の再生状態を次のターンへ持ち越さない
- 古いイベントや読み込み状態を引き継がない
- 長時間会話した後の音声出力の不安定化を減らす

新規作成の負荷は小さく、通常は体感速度に影響しない。

## 3. 再生終了後にAudioを破棄する

再生終了時には、次の後始末を行う。

1. 再生を止める
2. `onplay`、`onended`、`onerror`を解除する
3. MP3の`src`を外す
4. `load()`でメディア状態をリセットする
5. Blob URLを`URL.revokeObjectURL()`で解放する
6. アプリ側の参照`cloudAudio`を`null`にする

JavaScriptからOSの音声回路を強制的に即時解放することはできないが、ブラウザへ「この再生資源はもう不要」と伝えられる。

## 4. 音声認識もターンごとに作り直す

同じ`SpeechRecognition`を何度も`start()`すると、端末やブラウザによっては停止状態やマイク接続が残ることがある。

現在は自動会話の各ターンで新しい`SpeechRecognition`を作り、古い認識セッションを再利用しない。これにより、2～3ターン目で認識不能になる再現性の高い問題は改善した。

## 5. 音声認識イベントの意味

`SpeechRecognition`では、可能な場合に次のイベントが通知される。

| イベント | 意味 |
|---|---|
| `start` | 音声認識サービスが開始した |
| `audiostart` | マイク音声の取り込みが始まった |
| `soundstart` | 何らかの音を検出した |
| `speechstart` | 人間の声らしい音を検出した |
| `result` | 音声を文章として認識できた |
| `end` | 今回の認識セッションが終了した |

イベントは原因調査の表示だけに使うのではなく、次の回復処理を選ぶために使う。

### startのみでaudiostartがない

マイク入力が正常に始まらなかった可能性がある。

- 利用者の無言回数には含めない
- 「マイクを再接続しています」と表示する
- 認識オブジェクトを作り直して自動再接続する

### audiostartはあるがsoundstartがない

マイクは動作しているが音を拾っていない。

- 通常の無言として扱う
- 無言回数を増やす
- 次の認識を開始する

### soundstartはあるがspeechstartがない

食器、テレビ、音楽などの音は検出したが、人間の発話とは判断されなかった可能性がある。

- マイク故障とは判断しない
- 「周囲の音を確認しました」と表示する
- 最初の1回は無言回数に含めず、認識を作り直す

### speechstartはあるがresultがない

利用者の声は届いたが文章に変換できなかった。

- 利用者の無言とは扱わない
- 「声は届きました」と表示する
- 最初の1回は認識を作り直して再試行する

### resultがある

正常な認識である。

- 無言回数と回復済みフラグを0へ戻す
- 認識文章をAIへ送る

ブラウザによって一部イベントを通知しないことがある。そのため、イベントが1回欠けただけで永久ループさせず、特別な自動回復は1回に限定する。その後は従来の無言回数判定へ戻す。

## 6. 1.2秒の切り替え待ち

TTS終了から次の音声認識開始までの標準待ち時間は1.2秒を維持する。

約2秒は会話では長く感じるため、すべての利用者へ長い待ち時間を課す設計にはしない。通常時は1.2秒のまま、異常が疑われた場合だけ認識オブジェクトを作り直す。

## 7. テレビ・音楽・食器の音を認識しにくい理由

Bar Companion自身がテレビと利用者を完全に識別しているわけではない。端末、OS、ブラウザ、音声認識サービスが次の処理を行う。

1. ノイズ抑制
2. 音量の自動調整
3. エコーキャンセル
4. VAD（Voice Activity Detection）による声らしさの判定
5. 音声認識モデルによる文章化

食器の短い衝撃音、連続した音楽、遠くのテレビ音声などは、目の前の利用者の発話としての信頼度が低く、`result`まで進まないことがある。ただし、テレビの話し声が大きく明瞭なら誤認識する可能性はある。

## 8. 低性能端末の影響

AnTuTuの総合点やGPU性能だけでは音声認識の安定性を判断できない。

影響しやすい要素：

- CPUによるMP3デコードとJavaScript処理
- メモリ容量と空き容量
- AndroidやChromeのバージョン
- メーカー独自の省電力設定
- OSの音声フォーカス制御
- マイク品質とノイズ処理
- Wi-Fiやモバイル回線の安定性

端末が遅いとJavaScriptタイマーは通常、指定時間より遅れて動く。1.2秒が勝手に短くなるわけではない。性能不足は症状を起こしやすくする間接要因にはなるが、アプリ側で接続失敗を検出して回復する必要がある。

## 9. Chrome、Brave、iOSの違い

### Android Chrome

Bar Companion第1版の推奨ブラウザ。現在の音声認識実装と検証の基準とする。

### Android BraveなどのChromium系

描画エンジンがChromium系でも、音声認識サービス、APIキー、権限、プライバシー機能、ビルド設定までChromeと同一とは限らない。`SpeechRecognition`が見えても結果が返らない場合があるため、個別確認が必要である。

### iOS Safari

WebKit上で動き、Safari 14.1以降の音声認識はAppleの説明ではSiriと同じ音声認識エンジンを使用する。Android Chromeとは別実装であり、iOSの音声セッション制御の影響を受ける。

iOS上のChromeやBraveもAndroid版Chromeと同じものではない。iOSではWebKitおよびOS側の制約を受ける。またWKWebViewを使うアプリ内ブラウザでは、APIが見えても音声認識が動かない場合がある。

第1版の方針：

- Android：Chromeを推奨
- Androidの他ブラウザ：端末ごとに確認
- iOS：別対応として扱う

## 10. 参考資料

- [MDN: Web Speech APIの使い方](https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API/Using_the_Web_Speech_API)
- [MDN: SpeechRecognition speechstartイベント](https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition/speechstart_event)
- [WebKit: Safari 14.1の音声認識](https://webkit.org/blog/11648/new-webkit-features-in-safari-14-1/)
- [WebKit Bugzilla: iOS WKWebViewのWeb Speech API](https://bugs.webkit.org/show_bug.cgi?id=239816)

## 11. 実機確認項目

1. Android Chromeで10往復以上会話する
2. TTS終了後、1.2秒程度で次の認識が始まることを確認する
3. 声を出さなかった場合は通常の無言判定になることを確認する
4. テレビや音楽だけの場合の表示を確認する
5. 声が届いたが文章化できない場合、自動再接続することを確認する
6. 再接続後に正常な会話へ戻ることを確認する
7. 完全に無言の場合は、最終的に自動会話が停止することを確認する
